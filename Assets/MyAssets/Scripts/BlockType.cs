using UnityEngine;

public class BlockType : MonoBehaviour
{
    public enum Type {None, Würfel, Quader, Zylinder, HalbZylinder, Rampe, Querbalken, Brücke, Pyramidenquader, AngewinkelterQuader  };
    
    public Type type;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
